<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.sourceforgery.ekort.android.EkortActivity"
    tools:showIn="@layout/app_bar_ekort">

    <ProgressBar
        android:id="@+id/ekort_progress"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone" />

    <include
        android:id="@+id/past_transactions"
        layout="@layout/past_transaction_layout"
        android:visibility="gone" />

    <include
        android:id="@+id/active_ecards"
        layout="@layout/active_cards_layout"
        android:visibility="gone" />

    <include
        android:id="@+id/create_ecard_form"
        layout="@layout/create_ecard_layout" />
</android.support.constraint.ConstraintLayout>
